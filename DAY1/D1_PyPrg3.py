# Casting
# Create an integer variable num_int with a value of 10
num_int = 10

# Convert num_int into a float and store it in num_float
num_float = float(num_int)

# Convert num_int into a string and store it in num_str
num_str = str(num_int)

# Convert num1 integer into a bool and store it in num2bool 
num1 = 0
num2bool = bool(num1)

# Convert num1 integer into a float and store it in num2float 
num1 = 0
num2float = float(num1)

# Print the values and data types of num_float and num_str
print(f"Original integer: {num_int}, Type: {type(num_int)}")
print(f"Converted to float: {num_float}, Type: {type(num_float)}")
print(f"Converted to string: {num_str}, Type: {type(num_str)}")
print(f"Converted to boolean: {num2bool}, Type: {type(num2bool)}")
print(f"Converted to float: {num2float}, Type: {type(num2float)}")